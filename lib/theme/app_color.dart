import 'package:flutter/material.dart';

/// Application color palette
class AppColor {
  /// White color
  static const white = Color.fromRGBO(255, 255, 255, 1);

  /// Primary black color
  static const primaryBlack = Color.fromRGBO(0, 0, 0, 1);

  /// Red color
  static const red = Color.fromRGBO(161, 32, 10, 1);

  /// Error red color
  static const error = Color.fromRGBO(209, 0, 0, 1);

  /// Disabled state color
  static const disabledColor = Color.fromRGBO(153, 153, 153, 1);

  /// Secondary dim grey color
  static const secondaryDimGrey = Color.fromRGBO(109, 107, 107, 1);

  /// Medium grey color
  static const greyMedium = Color.fromRGBO(130, 128, 128, 1);

  /// Secondary dim grey background
  static const secondaryDimGreyBackground = Color.fromRGBO(237, 237, 237, 1);

  /// Secondary Spanish grey
  static const secondarySpanishGrey = Color.fromRGBO(153, 153, 153, 1);

  /// Chat date divider color
  static const chatDateDividerColor = Color.fromRGBO(194, 194, 194, 1);

  /// Chat secondary text color
  static const chatSecondaryColor = Color.fromRGBO(109, 107, 107, 1);

  /// Chat primary background color
  static const chatPrimaryColor = Color.fromRGBO(237, 237, 237, 1);

  /// Light grey color
  static const auLightGrey = Color.fromRGBO(227, 227, 227, 1);

  /// Grey color
  static const auGrey = Color.fromRGBO(203, 203, 203, 1);

  /// Grey background color
  static const auGreyBackground = Color.fromRGBO(46, 46, 46, 1);

  /// Quick silver grey color
  static const auQuickSilver = Color.fromRGBO(160, 160, 160, 1);

  /// Feral File highlight color
  static const feralFileHighlight = Color.fromRGBO(236, 255, 12, 1);

  /// Feral File light blue color
  static const feralFileLightBlue = Color.fromRGBO(185, 229, 255, 1);

  /// Feral File medium grey color
  static const feralFileMediumGrey = Color.fromRGBO(183, 183, 183, 1);

  /// Light red color
  static const lightRed = Color.fromRGBO(244, 67, 54, 1);

  /// Dark grey color
  static const darkGrey = Color.fromRGBO(46, 46, 46, 1);
}
